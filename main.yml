- name: Configure arch environment
  hosts: local

  roles:
    - name: host_info
      tags: ['host_info']

    - name: configure_console
      tags: ['configure_console']

    - name: timezone
      tags: ['timezone']

    - name: ntp
      tags: ['ntp']

    - name: locale
      tags: ['locale']

    - name: hostname
      tags: ['hostname']

    - name: configure_initramfs
      tags: ['configure_initramfs']

    - name: configure_pacman
      tags: ['configure_pacman']

    - name: install_packages
      tags: ['install_packages']

    - name: users
      tags: ['users']

    - name: configure_xdg
      tags: ['configure_xdg']

---
- name: Copy locale.conf
  ansible.builtin.template:
    src: locale.conf.j2
    dest: /etc/locale.conf
    owner: root
    group: root
    mode: '0644'
  become: true

- name: Ensure locales
  community.general.locale_gen:
    name: "{{ locales }}"
    state: present
  become: true

#!/usr/bin/env bash
set -eu

CONSOLE_FONT_DIR="/etc"
CONSOLE_FONT_FILE="vconsole.conf"

envsubst < ./${TEMPLATES_DIR}/${CONSOLE_FONT_FILE}.tmpl > ${TEMP_DIR}/${CONSOLE_FONT_FILE}
$SUDO mv ${TEMP_DIR}/${CONSOLE_FONT_FILE} ${CONSOLE_FONT_DIR}/${CONSOLE_FONT_FILE}
